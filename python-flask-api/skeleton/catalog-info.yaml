apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: Projeto Flask API com Python
  description: Flask
  annotations:
    github.com/project-slug: LuizRojas/${{ values.name }}
spec:
  type: service
  owner: ${{ values.owner }}
  lifecycle: experimental
